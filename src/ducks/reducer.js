const initialState = {
  name: '',
  address: '',
  city: '',
  state: '',
  zip: ''
}

function reducer(state = initialState, action) {
  switch (action.type) {
    default: 
      return state;
  }
}

export default reducer;